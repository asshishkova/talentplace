<div class="navbar navbar-expand-sm navbar-dark navbar-lewagon sticky_top">
  <div class="container-fluid">
    <div class="talent_place_btn links_blue_to_white ps-1">
        <%= link_to root_path do %>
          <span class="fas fa-gem"></span> TalentPlace
        <% end %>
    </div>

    <button class="navbar-toggler p-1" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if talent_signed_in? %>
          <%= render "partials/navbar_signed_in_talent" %>
        <% elsif place_signed_in? %>
          <%= render "partials/navbar_signed_in_place" %>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", "#", class: "avatar dropdown-toggle dashboard_navbar_btn", id: "navbarDropdown", data: { bs_toggle: "dropdown" }%>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "as a talent", new_talent_session_path, class: "dropdown-item" %>
              <%= link_to "as a place", new_place_session_path, class: "dropdown-item" %>
            </div>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", "#",  class: "avatar dropdown-toggle dashboard_navbar_btn", id: "navbarDropdown", data: { bs_toggle: "dropdown" }%>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "as a talent", new_talent_registration_path, class: "dropdown-item" %>
              <%= link_to "as a place", new_place_registration_path, class: "dropdown-item" %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
