<p>
Please note that the data on this page is not real, it is used to demonstrate the idea of the web-site only, all the rights belong to their owners.
</p>

<div class="light-background">
  <div class="container mt-5 mb-5 p-3"  >
    <div class="flex-space-between">
      <div>
        <h2><%= @talent.name %></h2>
        <% @talent.genres.each do |genre| %>
          <span class="badge rounded-pill bg-dark">#<%= genre.name %></span>
        <% end %>
        <p>
        <p><%= @talent.address %></p>
        <p>$<%= @talent.price %> per hour</p>
        <p><%= @talent.description %></p>
      </div>
      <div>
        <%= link_to "Book!", '#', class: "btn btn-flat" %>
      </div>
    </div>
    <% if @talent.photo.key %>
      <p><%= cl_image_tag @talent.photo.key, class: "img-fluid bg-white rounded shadow h-100" %></p>
    <% end %>
    <% youtube_link = "https://www.youtube.com/embed/#{@talent.youtube_link.gsub(/https:\/\/youtu.be\/(.+)/, '\1')}" %>
    <% instagram_link = "#{@talent.instagram_link}" %>
    <% spotify_link = "#{@talent.spotify_link}" %>
    <div>
      <iframe src=<%= youtube_link %>  title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    </div>
    <p>You can also see <%= @talent.name%>'s <%= link_to 'instagram', instagram_link, target: "_blank"%> and <%= link_to 'spotify', spotify_link, target: "_blank"%> pages.</p>
  <div class="flex-space-between container mt-5 mb-5">
    <div>
      <%= link_to 'Back to the list', talents_path, class: "btn btn-gradient" %>
    </div>
  </div>
</div>

<p>
Please note that the data on this page is not real, it is used to demonstrate the idea of the web-site only, all the rights belong to their owners.
</p>
